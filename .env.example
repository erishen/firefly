# 本地 LLM 代理配置（server/proxy.mjs 读取）
# 复制本文件为 .env 并填入你自己的值；.env 已被 .gitignore 忽略，不会入库。
# 支持任意 OpenAI 兼容服务（填入其 base / key / 模型名即可），前端不接触 key。

# OpenAI 兼容接口的 base（通常形如 https://your-endpoint/v1，不要带末尾斜杠也可）
LLM_BASE_URL=https://your-openai-compatible-endpoint/v1

# 服务商 API Key（仅服务端使用，前端不可见）
LLM_API_KEY=sk-your-key-here

# 模型名（按你所用服务填，例如 deepseek-chat / gpt-4o-mini 等）
LLM_MODEL=your-model-name

# 可选：采样温度，默认 0.8
LLM_TEMPERATURE=0.8

# 可选：代理监听端口，默认 8787
PORT=8787

# 可选：监听网卡地址。默认 127.0.0.1（仅本机），部署时由 nginx 反代到
# 127.0.0.1:8787 即可，端口不会直接暴露公网。一般无需改动。
# BIND_HOST=127.0.0.1

# 可选：CORS 白名单（逗号分隔的允许来源）。
# 留空 = 仅允许本地开发源（localhost:5173 / 127.0.0.1:8787），部署请务必设置！
# 例如部署到 firefly.erishen.cn：
#   ALLOWED_ORIGINS=https://firefly.erishen.cn
# 切勿设为 *（会让任意网站盗用你的 LLM Key 发起 /api/chat 请求）。
# ALLOWED_ORIGINS=

# 可选：上游走代理（Node 原生 fetch 不读系统 HTTP(S)_PROXY）
# 代理探测优先级：本项 LLM_PROXY > 进程环境变量 HTTPS_PROXY/HTTP_PROXY
#   > 系统代理（macOS scutil / Linux gsettings，本机 Terminal 跑 npm run dev 时自动用）。
# 也就是说：只要你本机系统代理已配（如 ClashX 在 127.0.0.1:7890），
# 通常【不用】手动填这项，proxy.mjs 启动时会自动探测并走代理。
# 只有自动探测失败或你想强制某个代理时，才显式写：
#   LLM_PROXY=http://127.0.0.1:7890
# 启用后会用 undici 的 ProxyAgent 仅对上游 LLM 请求做转发（本地 /api 不受影响）；
# 未安装 undici 请先 npm i undici（本项目 dependencies 已含）。
# LLM_PROXY=
